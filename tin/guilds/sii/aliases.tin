#alias use_corpse {
  !hp;
  !envelop corpse;
  !collect dna from corpse;
  !hp;
  !get all;
}

#alias f10 {use_corpse}

#alias reet {
  !unwrap corpse;
  !eat corpse;
}

#alias reinf {
  !unwrap corpse 1;
  !infest;
}

#alias reen {
  !unwrap corpse;
  !envelop corpse;
  !collect dna from corpse;
}
