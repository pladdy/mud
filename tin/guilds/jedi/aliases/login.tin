#alias fixup {
  run jedis;
  #send !n;
  #map move n;
  #send !nw;
  #map move nw;
  #send !repair $jedi_saber_color\;lightsaber;
  fixeq;
}

#alias linkdie_commands {
  #var mindblock no;
}

#alias login_commands {
  #var mindblock no;
}
