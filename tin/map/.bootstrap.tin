#nop Initialize the mapping stuff;

#var map_path $map_dir/map.txt;

#showme Setting up map class;
#class map kill;
#class map open;
read_tin_files $map_dir;
#class map close;

#nop Backup map;
#system cp $map_path $map_dir/backup_map.txt;

#map read $map_path;
map_lock;
refresh_map_on_enter;

#nop Go to last known room if possible;

#read last_session.tin;

#regexp {$map[current_room]} {{[0-9]+}} {
  #map goto $map[current_room]
} {
  #map goto
};
