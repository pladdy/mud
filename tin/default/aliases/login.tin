#alias linkdie_commands {#nop;}
#alias login_commands {#nop;}
#alias relog_commands {#nop;}

#alias quit {
  #var quit true;
  !quit;
  #gts #var quit true;
}

#alias shutdown_tmux {
  #system {tmux kill-session -t tintin};
}
