#alias f10 {
  !hp;
  !fdeener;
  !hp;
  !get all;
}

#alias f12 {
  #if {$map[current_room] == 11438} {
    #loop 2 8 i {!donate all from bag $i};
  } {
    #loop 2 8 i {!sell all from bag $i};
  }
}

#alias rede {
  !unwrap;
  !fdeener;
}
