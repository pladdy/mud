#nop #prompt and #showme are used in events to display information from 'showd';
#nop when it's invoked.  #showme is being used for my gentech hp monitor based;
#nop on the second hpbar triggering for example (guilds/gentech/actions.tin);

#alias showd {
  #split {} 1;
}

#alias hided {
  #unsplit;
}

#alias status_bar {
  #if {"%1" == "on"} { #split {} 2 } { #unsplit };
}

#alias hpbar_calcs {
  #nop do calcs for vars here: hp, sp, gp1, gp2 (last, pct, delta);
}

